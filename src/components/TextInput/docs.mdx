## Implicit props

* disabled
* placeholder

Should props be explicit, just to assure type checking?


## Explicit

* value - default to empty string to avoid 'undefined'



Icons need to be render within the "Label" to fit within the "Border"(generated through css)



